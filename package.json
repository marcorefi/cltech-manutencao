{
  "name": "gestorprev",
  "version": "2.0.0",
  "description": "GestorPrev - Dashboard da Prevenção (Vercel + Supabase)",
  "private": true,
  "engines": {
    "node": "20.x"
  },
  "scripts": {
    "dev": "vercel dev",
    "deploy": "vercel deploy --prod",
    "db:migrate": "node scripts/migrate.mjs",
    "db:seed": "node scripts/seed.mjs",
    "import:sheets": "node scripts/import-from-sheets.mjs"
  },
  "dependencies": {
    "bcryptjs": "^2.4.3",
    "pg": "^8.20.0"
  },
  "devDependencies": {
    "@types/node": "^20.11.0",
    "vercel": "^34.0.0"
  }
}
